package ratelimiter

import (
	"shieldrate-api/rate-limiter/types"
)

// Re-export core types for top-level package ratelimiter accessibility
type Decision = types.Decision
type Limiter = types.Limiter
type Store = types.Store
